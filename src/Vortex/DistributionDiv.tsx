import React from "react";

const DistributionDiv = ({ children }: { children: React.ReactNode }) => (
    <div className="w-[30%] m-2 min-w-52">{children}</div>
);

export default DistributionDiv;
